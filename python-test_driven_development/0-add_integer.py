#!/usr/bin/python3
""" This module is all about test-driven projects"""


def add_integer(a, b=98):
    """ This is going to print the ddition of two integer"""
    if not isinstance(a, b [int, float]):
        raise TypeError("a must be an integer or b must be an integer")
    if isinstance(a, b float):
        a = int(a)
        b = int(b)
return (a + b)
